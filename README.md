# yaiwes-factory-ui

Vercel project: `yaiwes-factory-ui`. Matte lock. No HF.

```bash
cd /workspace/yaiwes-factory-ui && npm install && npm run dev
```

Routes: /step-1 … /step-5. Stubs: POST /api/factory/run, GET /api/factory/status, POST /api/factory/deploy-prep.
localStorage: FROMTED_factory_input, FROMTED_factory_windows, FROMTED_factory_theme.
